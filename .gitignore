# build
dist/
.vercel/

# dependencies
node_modules/

# astro
.astro/

# env
.env
.env.local
.env.production

# os
.DS_Store
Thumbs.db

# editor
*.swp
*.swo
*~

# scraped content (generated by scripts/scrape.ts)
scraped/

# screenshots (browser testing artifacts)
screenshots/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# private advisory operations package (relationship notes, SOW drafts, prospect
# research). Never belongs in the public site repo or its build output.
advisory_practice_v2/

# private job-search material (tailored resumes, target employers, evidence
# ledger of unverified claims). Publishing this in the public site repo would
# announce an active job search to anyone who reads the commit log.
outreach-resume-work/
Jason_St_George_Resume_Template_Kit/

# retired artifacts kept locally, deliberately not served or published
archive/
